$(document).ready(function () {
/*
    /!*消除空格*!/
    var str = '     jquery  ';
    alert(str);
    alert($.trim(str));

    //筛选数据
    var arrGrep = [5,4,256,2,1,90];
    var result = $.grep(arrGrep,function (element,index) {
        return index<2 && element<5;
    });
    alert(result);

    //修改数据
    var arrMap = [5,4,256,2,1,90];
    var result = $.map(arrMap,function (element,index) {
        //return index<2 && element<5;//返回值是布尔值，整体返回数组
        if(index<5&&element<5){
            return element+1;
        }//修改了数据
    });
    alert(result);

    //查找元素下标
    var arrMap = [5,4,256,2,1,90];
    alert($.inArray(4,arrMap));

    //合并两个数组
    var arrMap = [5,4,256,2,1,90];
    var arrMap1 = [15,14,56,12,1,90];
    alert($.merge(arrMap,arrMap1));

    //删除重复的DOM元素
    var divs = $('div').get();
    alert(divs.length);
    divs = divs.concat($('.box').get());//连接数组
    alert(divs.length);
    $.unique(divs);
    alert($(divs).length);

    //合并多个DOM组成数组
    alert($('div').toArray().length);

*/



});